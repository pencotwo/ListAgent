{
  "name": "bios-build",
  "display_name": "BIOS Build",
  "description": "Use this skill for BIOS build requests: build BIOS, compile BIOS, clean build, rebuild, or clean BIOS. Clean-build/rebuild/clean-first requests run clean.bat before building.",
  "language": "any",
  "model": "any",
  "version": "1.0.8",
  "system_prompt": "BIOS Build Skill: build or compile BIOS.\n\nCLEAN BUILD: if the request is a clean build, rebuild, or asks to clean first, run clean.bat before building: run_shell_command `cmd /c clean.bat` and wait until it finishes. A plain build request skips this step.\n\nWORKFLOW:\n1. Clear log: run_shell_command `Remove-Item Build.log -ErrorAction SilentlyContinue; New-Item Build.log -ItemType File -Force | Out-Null`.\n2. Launch non-blocking: run_shell_command `Start-Process -FilePath 'cmd.exe' -ArgumentList '/c build.bat > Build.log 2>&1 & (if errorlevel 1 (echo BUILD_FAILED >> Build.log) else (echo BUILD_SUCCESS >> Build.log))' -NoNewWindow`.\n3. Monitor loop: wait(seconds=180), then run_shell_command `Get-Content Build.log -Tail 20`. Judge only by markers: `*** BIOS compilation successful! Output:` means SUCCESS; `BUILD_FAILED` means FAILED; neither means still building, so keep waiting.\n\nRULES: never run build.bat directly because the shell tool can time out. For clean build, run clean.bat first and only start build.bat after it completes. Never give up while still building. If no marker appears after about 60 minutes, report the log tail and ask. On success, report duration if `Build end time:` appears.",
  "custom_tools": []
}
